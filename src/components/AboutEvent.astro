---
import sect1Webm from '../assets/video-sect1.webm';
import sect1Mp4 from '../assets/video-sect1.mp4';
import sect2Mp4 from '../assets/video-section2.mp4';
import sect3Mp4 from '../assets/Video-Sect3-webm.mp4';

type Slide = { title: string; text: string; webm?: string; mp4?: string };

const slides: Slide[] = [
	{ title: 'Aprende junto a Luquin', text: 'Una jornada de capacitación intensiva con Josué Luquin para dominar las técnicas de maquillaje más avanzadas del mercado, actualizar tus servicios y elevar el valor de tu trabajo.', webm: sect1Webm, mp4: sect1Mp4 },
	{ title: 'Experiencia & Comunidad', text: 'Más que un evento, un punto de encuentro aspiracional. Disfrutá de un ambiente premium donde la comunidad beauty se reúne a crear alianzas, inspirarse y celebrar el arte del maquillaje en una jornada inolvidable.', mp4: sect2Mp4 },
	{ title: 'Explorá las mejores Marcas & Novedades', text: 'Explorá y testeá insumos de primer nivel antes que nadie. Un espacio interactivo para conocer lanzamientos, probar fórmulas en directo y renovar tu kit de productos con las firmas más destacadas de la industria.', mp4: sect3Mp4 },
];
---

<!-- ¿De qué trata el evento? -->
<section id="evento" class="py-16 sm:py-24">
	<div class="mx-auto max-w-6xl px-4">
		<div class="mx-auto max-w-2xl text-center">
			<h2 class="font-display text-3xl font-bold tracking-tight sm:text-4xl">La Experiencia Beauty del Año</h2>
			<p class="mt-4 text-base text-neutral-300 sm:text-lg">
				Una experiencia exclusiva en el Hilton Mendoza junto a Josué Luquin, que reúne técnica, inspiración, networking y las mejores marcas de la industria beauty.

Deslizá y descubrí la experiencia.
			</p>
		</div>

		<div class="mt-12 flex snap-x snap-mandatory gap-6 overflow-x-auto pb-4" data-autocarousel>
			{
				slides.map((slide) => (
					<article class="flex w-full shrink-0 snap-center flex-col gap-4 sm:flex-row sm:items-center sm:gap-8">
						<div class="sm:w-1/2">
							<h3 class="text-xl font-semibold">{slide.title}</h3>
							<p class="mt-2 text-base text-neutral-300">{slide.text}</p>
						</div>
						{slide.webm || slide.mp4 ? (
							<video
								muted
								loop
								playsinline
								preload="metadata"
								class="aspect-video w-full rounded-2xl bg-neutral-800 object-cover sm:w-1/2"
							>
								{slide.webm && <source src={slide.webm} type="video/webm" />}
								{slide.mp4 && <source src={slide.mp4} type="video/mp4" />}
							</video>
						) : (
							<div class="aspect-video w-full rounded-2xl bg-neutral-800 sm:w-1/2" />
						)}
					</article>
				))
			}
		</div>
	</div>
</section>
<!-- End ¿De qué trata el evento? -->

<script>
	const carousel = document.querySelector<HTMLElement>('[data-autocarousel]');
	const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

	// Reproduce automáticamente solo el video del slide visible y pausa el resto
	if (carousel && !reduceMotion) {
		const videos = carousel.querySelectorAll('video');

		if (videos.length) {
			const io = new IntersectionObserver(
				(entries) => {
					entries.forEach((entry) => {
						const video = entry.target as HTMLVideoElement;
						if (entry.isIntersecting) {
							video.play().catch(() => {});
						} else {
							video.pause();
						}
					});
				},
				{ threshold: 0.6 },
			);

			videos.forEach((video) => io.observe(video));
		}
	}

	if (carousel && !reduceMotion) {
		const slides = Array.from(carousel.children) as HTMLElement[];

		if (slides.length > 1) {
			let index = 0;
			let timer: number | undefined;

			const go = (i: number) => {
				index = (i + slides.length) % slides.length;
				carousel.scrollTo({
					left: slides[index].offsetLeft - carousel.offsetLeft,
					behavior: 'smooth',
				});
			};

			const start = () => {
				stop();
				timer = window.setInterval(() => go(index + 1), 6000);
			};

			const stop = () => window.clearInterval(timer);

			// Resincroniza el índice con el slide visible tras deslizar manualmente
			let scrollTimer: number | undefined;
			const syncIndex = () => {
				const pos = carousel.scrollLeft + carousel.offsetLeft;
				index = slides.reduce(
					(best, slide, i) =>
						Math.abs(slide.offsetLeft - pos) < Math.abs(slides[best].offsetLeft - pos) ? i : best,
					0,
				);
			};

			carousel.addEventListener('scroll', () => {
				window.clearTimeout(scrollTimer);
				scrollTimer = window.setTimeout(syncIndex, 150);
			});

			// Pausa mientras el usuario interactúa
			carousel.addEventListener('mouseenter', stop);
			carousel.addEventListener('mouseleave', start);
			carousel.addEventListener('pointerdown', stop);
			carousel.addEventListener('pointerup', start);

			start();
		}
	}
</script>
