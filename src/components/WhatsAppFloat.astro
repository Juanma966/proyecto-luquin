---
import { Icon } from 'astro-icon/components';

interface Props {
	message: string;
	phone?: string;
}

const { message, phone = '5492645739387' } = Astro.props;
const href = `https://wa.me/${phone}?text=${encodeURIComponent(message)}`;
---

<!-- Botón flotante de WhatsApp -->
<a
	href={href}
	target="_blank"
	rel="noopener noreferrer"
	aria-label="Contactar por WhatsApp"
	class="fixed right-5 bottom-5 z-50 flex h-14 w-14 items-center justify-center rounded-full bg-[#25D366] text-white shadow-lg transition hover:scale-105 sm:right-6 sm:bottom-6"
>
	<Icon name="simple-icons:whatsapp" class="h-7 w-7" />
</a>
<!-- End Botón flotante de WhatsApp -->
